    #include <stdio.h>

    int main(void) {
        int first, second;
        scanf("%d", &first);
        getchar();
        scanf("%d", &second);
        getchar();
        int sum = first + second;
        printf("%d", sum);
        return 0;
    }